# Team Chat — Monolith — 13-05-2026

## Session Start — Phase 2B Schema + Migration (MON-11/12/13 + MON-13b spinoff)

**Mode:** Executed in main context (AM/MT) after rate-limit-induced subagent failure 12-05-2026
**Source of truth:** `Development/Web_repair_center/04_Modification Logs/AdminProgramEnhancement_12-05-2026/`

### Voices

**AT (Conductor):**
ดำเนินการในบริบทหลักครับ — Monolith subagent ติด rate limit รอบแรก, รอบสอง API internal error
ทำให้เสีย time window. AM ตัดสินใจรันใน main context เอง เพื่อ deterministic + ทดสอบเรียลไทม์ได้
3 ตั๋ว (MON-11/12/13) ทำเสร็จครบ, แตกตั๋วใหม่ MON-13b เพื่อ defer การ drop column ตามแบบ Phase 0.5d

**SC (Technologist):**
Schema deltas ทั้ง 4 ลง Supabase แล้ว: `JobCategory String?` + `SupplierName String?` + new model `TicketTechnician` + Technician (เก็บ dual-write ไว้)
Migration script idempotent — รัน 2 รอบ, รอบ 2 = 0 inserts, 0 reclassifications. Zero data loss verified
Server actions ใหม่ 4 ตัว: `updateTicketCategory`, `updateTicketSupplier`, `assignTechnicians`, plus extended `updateTicketStatus` รับ array
TS strict pass — `npx tsc --noEmit` ครบไฟล์ ไม่มี error

**EN (Verification Scholar — PF):**
Production data state verified post-migration (R-01 PASS):
- 89 legacy non-null Technician rows accounted: 56 persons + 21 contractor + 12 empty-string
- 56 → TicketTechnician (zero loss)
- 21 → JobCategory="ช่างรับเหมา" reclassification (zero loss)
- 12 empty-strings skipped intentionally (logged in script)
Regression test plan filed: `Development/Web_repair_center/09_TestCase/_regression/MON-13_TechnicianMigration.md`

### Actions Taken
1. ✅ Edited `prisma/schema.prisma` — added JobCategory, SupplierName fields + TicketTechnician model + indexes; KEEP Technician for dual-write
2. ✅ Created `src/lib/jobCategories.ts` (8 categories), `src/lib/suppliers.ts` (3 + free-text), `src/lib/technicians.ts` (8 — "ทีมช่างรับเหมา" REMOVED), `src/lib/statuses.ts` (7 statuses incl. WrongCategory)
3. ✅ Created `scripts/snapshot-technicians.mjs` (pre-migration evidence helper)
4. ✅ Created `scripts/migrate-technicians.mjs` — idempotent, handles 3 buckets (person/contractor/empty)
5. ✅ Killed live `node` processes to release Prisma DLL lock (Commander auth)
6. ✅ `npx prisma generate` (validation) → `npx prisma db push` (step 1) → `node scripts/migrate-technicians.mjs` → re-run idempotency check
7. ✅ Refactored `src/app/actions/tickets.ts`:
   - Helpers: `normalizeTechnicianNames`, `syncTicketTechnicians`
   - Extended `updateTicketStatus` to dual-write + handle "ทีมช่างรับเหมา" → JobCategory
   - New: `updateTicketCategory`, `updateTicketSupplier`, `assignTechnicians`
   - Updated `getAllTickets`, `getBranchTickets`, `getTechnicianTickets` to include `Technicians` relation
8. ✅ `npx tsc --noEmit` PASS (zero errors)
9. ✅ Created MON-13b ticket (drop column deferred 1-2 weeks post-ARC-14 ship)
10. ✅ Created regression test file `09_TestCase/_regression/MON-13_TechnicianMigration.md` (R-01..R-06)
11. ✅ Marked MON-11/12/13 status COMPLETE

### Output Delivered

**Files created:**
- `ticket-system/src/lib/jobCategories.ts`
- `ticket-system/src/lib/suppliers.ts`
- `ticket-system/src/lib/technicians.ts`
- `ticket-system/src/lib/statuses.ts`
- `ticket-system/scripts/snapshot-technicians.mjs`
- `ticket-system/scripts/migrate-technicians.mjs`
- `Development/Web_repair_center/04_Modification Logs/AdminProgramEnhancement_12-05-2026/MON-13b_DropTechnicianColumn.md`
- `Development/Web_repair_center/09_TestCase/_regression/MON-13_TechnicianMigration.md`

**Files modified:**
- `ticket-system/prisma/schema.prisma` (RepairTicket extended, TicketTechnician model added, indexes added)
- `ticket-system/src/app/actions/tickets.ts` (3 read fns, 1 extended write fn, 3 new write fns)
- 3 ticket files marked COMPLETE

**Reason:** MON-11/12/13 deliverables landed; MON-13b spun off because pure single-shot column drop violates Commander's documented stability preference (Phase 0.5d pattern requires observation window). Server actions now back-compat: legacy single-tech UI still works, new multi-tech UI (Arcade) can land without breaking old flow.

### Signal to Dependent Teams
- **Arcade (ARC-11/12/14/15):** all schema/types live in production. Import constants from `@/lib/jobCategories`, `@/lib/suppliers`, `@/lib/technicians`, `@/lib/statuses`. New actions ready: `updateTicketCategory`, `updateTicketSupplier`, `assignTechnicians`. **WrongCategory** is now a valid `CurrentStatus` value.
- **Syndicate (SYN-12):** AuditLog hooks already inline in the new server actions (using existing `logAudit` helper). SYN-12 work reduces to: verify hook coverage + add admin audit-log viewer if needed (out-of-scope deferred OK).

### Blockers / Tech Debt
- **MON-13b** — drop legacy `Technician` column after ARC-14 ships + 1-2 week observation
- **TD-002** (preexisting from Phase 1) — npm audit fix --force still pending Commander decision

### Closing
Status: All 3 tickets COMPLETE. Authorization HOLD. Returning to AM for aggregation + Phase 2 (Arcade + Syndicate) dispatch.
